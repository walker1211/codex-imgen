{
  "skill_name": "imgen",
  "evals": [
    {
      "id": 1,
      "prompt": "我想用 imgen 生成 4 张图片，并发 2，主题是五更琉璃穿着女仆装在咖啡馆。先不要真的执行，给我最小命令。",
      "expected_output": "Return a minimal ./imgen command using --count 4 and --concurrency 2, quote the Chinese prompt safely, and avoid extra setup steps unless needed.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "我有 ./1.png 和 ./2.png，想用第一张做主体、第二张做风格参考，生成一张统一风格的图。给 OpenClaw 一份可以复用的调用说明，不要依赖 Claude 工具。",
      "expected_output": "Explain platform-neutral CLI usage with repeated --image flags, include the exact ./imgen command, mention local file paths only, define how to find repo-root/imgen, and avoid Claude-specific tool names.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "imgen serve 里有个 job 一直很慢，我有 job-id。请告诉我排查顺序和 sqlite 查询语句。",
      "expected_output": "Ask for the job id if it is missing; otherwise give status/get/list, attempt SQL, phase SQL, logs/out.log, and native backend verification in that order without assuming unsupported ccs codex flags.",
      "files": []
    },
    {
      "id": 4,
      "prompt": "我想用 imgen 生成 3 张带中文标题的公众号横向封面候选，标题是『终于敢清理我的 zsh history 了』，并发 2。请给我最小命令，不要真的执行。",
      "expected_output": "Return a minimal ./imgen command using --count 3 and --concurrency 2. The prompt inside the command should request one horizontal cover candidate or single image, include the title and style constraints, and must not also ask for three images or three different compositions.",
      "files": []
    },
    {
      "id": 5,
      "prompt": "OpenClaw 当前 cwd 和 ~/.openclaw 都没有 ./imgen，也没找到 codex-imgen 仓库。请给它一份健壮的 repo-root 发现和失败提示，不要假设 <repo-root> 已知。",
      "expected_output": "Provide an ordered repo-root discovery contract using IMGEN_REPO_ROOT, upward search for configs/config.yaml plus imgen/build.sh/go.mod, explicit or common local checkout paths, and command -v imgen only when paired with a discovered config cwd. Include a clear 'imgen not installed' error and do not tell OpenClaw to run ./imgen from an unknown cwd.",
      "files": []
    },
    {
      "id": 6,
      "prompt": "本机只有 ccs codex，ccs codex exec --json 会 unknown option。请更新给 OpenClaw 的底层验证建议，避免照搬 codex exec --json。",
      "expected_output": "State that ./imgen currently expects backend.command to support 'exec --json'; explain ccs codex may not be compatible if it lacks --json; suggest verifying ccs codex help/capability first and using ./imgen --json or service mode as the stable contract instead of treating ccs codex '$imagegen' exit code 0 as success.",
      "files": []
    },
    {
      "id": 7,
      "prompt": "ccs codex '$imagegen ...' 退出码是 0，但没有输出图片路径，也没在工作区生成图片。怎么判断 imgen 是否成功，图片路径从哪里拿？",
      "expected_output": "Explain that imgen success requires image paths in text output or images[].path in JSON; native backend success requires a Saved to/file URI or generated_images detection, not just exit code 0. Mention the generated image discovery location and the 'image path not found' failure.",
      "files": []
    },
    {
      "id": 8,
      "prompt": "请告诉我 imgen 实际用什么模型生图。如果本地没有 repo/config，应该怎么回答和怎么确认？",
      "expected_output": "Say the model is determined by configs/config.yaml backend.model when present, otherwise by the Codex backend default; provide commands or file locations to inspect backend.model and backend.command; do not invent a model when repo/config is unavailable.",
      "files": []
    },
    {
      "id": 9,
      "prompt": "OpenClaw 只是要给当前 Telegram 聊天生成一张 PNG 壁纸并发回原图。它是不是要先启动 imgen serve？请给调用和回复规则。",
      "expected_output": "State that normal one-shot OpenClaw image generation should default to synchronous ./imgen --json from the discovered repo/config root and does not require imgen serve. Require success to include ok true and images[].path, then send the exact local file with the OpenClaw message tool using forceDocument/asDocument for original PNG quality, and reply only NO_REPLY after direct delivery. Do not suggest image_generate, direct codex exec fallback, or reusing old paths.",
      "files": []
    },
    {
      "id": 10,
      "prompt": "什么时候 OpenClaw 才应该用 imgen submit 或 WebSocket realtime，而不是直接 ./imgen --json？",
      "expected_output": "Explain that ./imgen --json synchronous CLI is the default for direct image paths. Use submit/service only when the caller needs server-side job management, polling, cancellation, or recovery. Use /v1/realtime/generate/ws only when imgen serve is running and the caller needs live streaming events for a WebSocket session; realtime should not create submit jobs or store rows. Mention that submit and realtime share scheduler.global_max_concurrency inside serve, while sync CLI does not.",
      "files": []
    },
    {
      "id": 11,
      "prompt": "./imgen --json 返回 ok=true、顶层 status=completed，但 images[0].status=done 且有 path。OpenClaw 应该判成功还是失败？",
      "expected_output": "Treat this as successful when ok is true and the expected images contain non-empty path values. Do not require per-image status to be completed; synchronous CLI image entries may use status done. Verify the local path exists before delivery, then send that exact path.",
      "files": []
    },
    {
      "id": 12,
      "prompt": "OpenClaw 要给当前 Telegram 聊天生成一张 PNG Mac 壁纸并直接发回原图。请给可执行调用和回复规则，不要真的生图。",
      "expected_output": "Set IMGEN_DELIVERY_DIR=\"${OPENCLAW_STATE_DIR:-$HOME/.openclaw}/workspace/imgen\" and run ./imgen --json from the discovered repo/config root. Treat success as ok true with non-empty images[].path, allowing images[].status done, send the exact returned local path via OpenClaw message using forceDocument/asDocument for document/original PNG delivery, then reply NO_REPLY. Do not call image_generate, do not fall back to direct codex exec, and do not send stale generated_images paths.",
      "files": []
    }
  ]
}
