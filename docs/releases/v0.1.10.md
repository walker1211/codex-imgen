## Highlights

- Added Codex as a default `skill-sync` install target at `~/.codex/skills/imgen`.
- Added `--codex-dir` for non-default Codex skill install locations.
- Updated README Skill Sync docs to distinguish the Claude source, OpenClaw repository mirror, and local Claude/OpenClaw/Codex installs.
- Added a README demo for OpenClaw/TG original-file delivery.
- Added GitHub public-readiness checks for key repository security settings.

## Notes

This release expands skill installation coverage so one `skill-sync --apply` updates Claude, OpenClaw, and Codex local skill directories.

## Validation

- `./scripts/ci-local.sh worktree`
- GitHub PR checks: `test`, `Analyze Go`, and `CodeQL`
